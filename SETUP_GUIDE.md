# POE2 런처 설치 및 설정 가이드

## 📋 목차
1. [시스템 요구사항](#시스템-요구사항)
2. [빌드 방법 (exe 파일 만들기)](#빌드-방법)
3. [계정 설정 방법](#계정-설정-방법)
4. [실행 방법](#실행-방법)
5. [문제 해결](#문제-해결)

---

## 시스템 요구사항

### 필수 사항:
- ✅ **운영체제**: Windows 10/11 (64bit)
- ✅ **Chrome 브라우저**: 최신 버전 설치 필수
  - [Chrome 다운로드](https://www.google.com/chrome/)
- ✅ **다음 계정**: Path of Exile 2 게임에 가입된 다음 계정
- ✅ **Python 3.8 이상** (빌드 시에만 필요)
  - [Python 다운로드](https://www.python.org/downloads/)
  - 설치 시 "Add Python to PATH" 옵션 체크!

---

## 빌드 방법

### Windows에서 exe 파일 만들기

#### 1단계: 프로젝트 다운로드
```cmd
git clone https://github.com/predebbit-crypto/POE2Launch.git
cd POE2Launch
```

또는 GitHub에서 ZIP 파일로 다운로드 후 압축 해제

#### 2단계: 빌드 실행
```cmd
build.bat
```

빌드 스크립트가 자동으로:
1. 필요한 라이브러리 설치
2. POE2_Launcher.exe 생성
3. POE2_Setup.exe 생성 (계정 설정 도구)
4. config.json 예시 파일 복사

빌드가 완료되면 `dist` 폴더에 다음 파일들이 생성됩니다:
- `POE2_Launcher.exe` - 메인 런처
- `POE2_Setup.exe` - 계정 설정 도구
- `config.json.example` - 설정 예시 파일

#### 3단계: 파일 확인
```
dist/
├── POE2_Launcher.exe    (메인 런처 - 약 10-15MB)
├── POE2_Setup.exe       (설정 도구 - 약 10MB)
└── config.json.example
```

---

## 계정 설정 방법

계정 정보를 설정하는 방법은 **2가지**가 있습니다:

### 방법 1: POE2_Setup.exe 사용 (권장) ⭐

가장 쉬운 방법입니다!

1. **`dist` 폴더**로 이동
2. **`POE2_Setup.exe`** 더블클릭
3. 안내에 따라 입력:
   ```
   다음 아이디: your_daum_id
   비밀번호: ••••••••
   비밀번호 확인: ••••••••
   ```
4. Enter 키를 눌러 완료!

**화면 예시:**
```
============================================================
  POE2 Auto Login Launcher - 계정 설정
============================================================

📝 다음 계정 정보를 입력해주세요.
   (Path of Exile 2에 로그인할 때 사용하는 다음 계정)

다음 아이디: myaccount
비밀번호:
비밀번호 확인:

============================================================
✅ 설정이 저장되었습니다!
============================================================

📁 설정 파일 위치: C:\...\dist\config.json
👤 저장된 아이디: myaccount

⚠️  보안 주의사항:
   - config.json 파일에는 비밀번호가 평문으로 저장됩니다
   - 이 파일을 다른 사람과 공유하지 마세요

🚀 이제 POE2_Launcher를 실행할 수 있습니다!
```

---

### 방법 2: config.json 수동 편집

텍스트 편집이 익숙하다면 이 방법을 사용할 수 있습니다.

#### 2-1. 메모장으로 설정 파일 만들기

1. **`dist` 폴더**로 이동
2. **메모장(Notepad)** 실행
3. 다음 내용을 **정확히** 입력:

```json
{
  "username": "your_daum_id",
  "password": "your_password"
}
```

4. `username`과 `password`를 실제 계정 정보로 변경:
   - `your_daum_id` → 다음 아이디
   - `your_password` → 다음 비밀번호

**예시:**
```json
{
  "username": "hong123",
  "password": "myP@ssw0rd!"
}
```

5. **다른 이름으로 저장** → 파일명: `config.json`
6. **파일 형식**: `모든 파일 (*.*)`로 변경!
7. **인코딩**: `UTF-8`로 설정
8. `dist` 폴더에 저장

#### 2-2. 주의사항

❌ **잘못된 예시:**
```json
{
  username: hong123,        # 따옴표 없음 (잘못됨!)
  "password": myP@ssw0rd!   # 따옴표 없음 (잘못됨!)
}
```

✅ **올바른 예시:**
```json
{
  "username": "hong123",
  "password": "myP@ssw0rd!"
}
```

**중요 체크리스트:**
- [ ] `username`과 `password`에 큰따옴표(`"`) 사용
- [ ] 값(아이디, 비밀번호)도 큰따옴표로 감싸기
- [ ] 각 줄 끝에 쉼표(`,`) 있는지 확인 (마지막 줄 제외)
- [ ] 파일 이름이 정확히 `config.json`인지 확인
- [ ] `config.json.txt`가 아닌지 확인!

---

## 실행 방법

### 1단계: 계정 설정 확인
- `dist` 폴더에 `config.json` 파일이 있는지 확인
- 파일을 열어서 계정 정보가 제대로 들어갔는지 확인

### 2단계: 런처 실행
1. `dist` 폴더의 **`POE2_Launcher.exe`** 더블클릭
2. Chrome 브라우저가 자동으로 열립니다
3. 자동으로 다음 작업이 진행됩니다:
   - POE2 사이트 접속
   - 계정 로그인
   - 게임시작 버튼 클릭

### 3단계: 게임 시작
- 게임 런처가 자동으로 실행됩니다
- 브라우저 창은 그대로 유지됩니다 (닫지 마세요)
- 게임 런처에서 게임을 시작하세요!

**실행 화면 예시:**
```
==================================================
🎯 POE2 Auto Login Launcher 시작
==================================================
🔧 브라우저 설정 중...
✅ 브라우저 설정 완료
🌐 웹사이트 열기: https://pathofexile2.game.daum.net/
✅ 웹사이트 로드 완료
🔐 로그인 시도 중...
✅ 로그인 버튼 클릭
✅ 아이디 입력 완료
✅ 비밀번호 입력 완료
✅ 로그인 제출
✅ 로그인 완료
🎮 게임 시작 버튼 찾기...
✅ 게임 시작 버튼 클릭 완료

==================================================
✨ 런처 작업 완료!
게임 런처가 실행될 때까지 브라우저를 유지합니다.
종료하려면 브라우저를 닫거나 Ctrl+C를 누르세요.
==================================================
```

---

## 문제 해결

### ❓ "config.json을 찾을 수 없습니다" 오류

**원인:** config.json 파일이 없거나 위치가 잘못됨

**해결 방법:**
1. `POE2_Setup.exe`를 실행하여 설정 파일 생성
2. 또는 `config.json` 파일이 `POE2_Launcher.exe`와 **같은 폴더**에 있는지 확인

### ❓ "username 또는 password가 없습니다" 오류

**원인:** config.json 파일 형식이 잘못됨

**해결 방법:**
1. config.json 파일을 메모장으로 열기
2. 형식 확인:
   ```json
   {
     "username": "아이디",
     "password": "비밀번호"
   }
   ```
3. 따옴표(`"`)가 모두 있는지 확인
4. 저장 시 인코딩을 UTF-8로 설정

### ❓ Chrome 드라이버 오류

**원인:** Chrome 브라우저가 설치되지 않았거나 버전이 너무 오래됨

**해결 방법:**
1. Chrome 브라우저 최신 버전 설치
2. Chrome 업데이트: `설정` → `Chrome 정보` → 자동 업데이트

### ❓ 로그인이 안 되는 경우

**가능한 원인:**
- 아이디/비밀번호가 틀림
- 2단계 인증 사용 중
- 캡차 인증 필요

**해결 방법:**
1. config.json의 계정 정보가 정확한지 확인
2. 2단계 인증이 있다면: 프로그램이 30초 대기하므로 그 동안 수동으로 인증
3. 캡차가 나타나면: 수동으로 풀기

### ❓ "게임시작 버튼을 찾을 수 없습니다" 메시지

**원인:** 웹사이트 구조 변경 또는 로그인 실패

**해결 방법:**
1. 브라우저 창이 열려있으므로 수동으로 게임시작 버튼 클릭
2. 로그인이 제대로 되었는지 확인

### ❓ 바이러스 경고가 뜨는 경우

**원인:** PyInstaller로 만든 실행 파일은 종종 백신 프로그램이 오탐지함

**해결 방법:**
1. 소스 코드를 직접 확인 (poe2_launcher.py)
2. 백신 프로그램에서 예외 추가
3. 또는 Python으로 직접 실행:
   ```cmd
   python poe2_launcher.py
   ```

---

## 보안 주의사항 🔒

⚠️ **중요:**
- `config.json` 파일에는 **비밀번호가 평문**으로 저장됩니다
- 이 파일을 **절대 다른 사람과 공유하지 마세요**
- **공용 컴퓨터**에서는 사용을 권장하지 않습니다
- 사용 후에는 `config.json` 파일을 **삭제**하는 것을 권장합니다

---

## 추가 도움말

### Python으로 직접 실행하기 (개발자용)

exe 파일 없이 Python으로 직접 실행할 수 있습니다:

```cmd
# 1. 의존성 설치
pip install -r requirements.txt

# 2. 설정
python setup_config.py

# 3. 실행
python poe2_launcher.py
```

### 업데이트 방법

새 버전이 나왔을 때:
1. 최신 코드 다운로드
2. `build.bat` 다시 실행
3. 기존 `config.json` 파일을 새 `dist` 폴더로 복사

---

## 파일 설명

| 파일 | 용도 | 크기 |
|------|------|------|
| `POE2_Launcher.exe` | 메인 런처 프로그램 | ~10-15MB |
| `POE2_Setup.exe` | 계정 설정 도구 | ~10MB |
| `config.json` | 계정 정보 (직접 생성) | <1KB |
| `config.json.example` | 설정 파일 예시 | <1KB |

---

## 연락처 및 지원

문제가 계속되거나 도움이 필요하시면:
- GitHub Issues에 문의
- 소스 코드 확인: `poe2_launcher.py`

---

**즐거운 게임 되세요! 🎮**
